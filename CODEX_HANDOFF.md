# NightWatch frontend/backend handoff

## 🤝 v0.1.22 — COORDINATION (backend lane, read before you push)

**Phase 23 backend is done** and sits on `backend/phase-23-profiles`, rebased on current `main`, typecheck + 41 tests + Activity build green. It implements all four contracts you asked for in `PHASE_23_SOCIAL_UI_BACKEND_HANDOFF.md`. I confirmed none of them existed — this was a real gap, not a duplicate.

### The sequence for v0.1.22

1. **Finish your small fixes and push.** I will not touch your files: my only change to existing code is `FriendService.ts` (two appended fields). Everything else is new.
2. **Both PRs merge** (they need the `automerge` label under your new review gate — noted, and I have adopted it).
3. **Blast applies `0020` and runs `supabase/tests/phase23_profiles_test.sql`.** ⚠️ **Nothing works until then**: without the migration every new RPC returns `not-ready` and the features hide themselves. That is the designed failure mode — but it means releasing before the migration ships nothing.
4. **Release v0.1.22.**

### What to build against

```ts
import {
  getSocialProfile, listBlockedUsers, getConversationMembers,
  inviteFriendToRoom, respondToRoomInvite, revokeRoomInvite, listRoomInvites,
  setProfileAvatar, setShareAchievements,
} from '@/lib/social/SocialProfileService';
```

`Relation` (from `getSocialGraph()`) **now carries `avatarUrl` and `selectedBorderId`**, so friend lists render avatars and borders with no extra call.

### Five things that will bite you if you assume otherwise

1. **A field's absence IS the answer.** If `stats`, `achievements`, or `mutualRooms` is missing, you were not permitted to see it. There is no client-side way to recover it — and show *nothing*, not "0 hours watched", which is a lie about a person.
2. **A blocked user raises `blocked`, not an empty profile.** The existence of the row is itself information. Render the block state, never a profile shell.
3. **Stats and achievements are SEPARATE opt-ins.** Do not build one toggle for both: agreeing to show watch time must not silently publish what someone has unlocked.
4. **Borders are validated server-side.** `selectedBorderId: null` means it was never unlocked. Do not fall back to the raw column — that is the forgery path the validation closes.
5. **Avatars must be Discord CDN URLs**; the column enforces it. An arbitrary URL rendered as an `<img>` in other users' clients is a tracking beacon that hands the setter every viewer's IP.

### Two deletions you can now make

- **The shortened-UUID fallback** for non-friend group members — `getConversationMembers()` returns real names, avatars, and borders. That fallback was itself a small leak: a raw user id is a stable cross-room identifier.
- **Any client-side block shadow list** — `listBlockedUsers()` is authoritative. The shadow list was already wrong the moment someone blocked on a second device.

### Invitations

7-day expiry, revocable, audited, 20/day. An **expired invitation is gone, not merely old**: it will not list and cannot be accepted. Accepting is what grants room access — the invitation is only a request.

---


Last updated: 2026-07-12 after the `v0.1.21` tag (`f34963e`).

## Read this first

- NightWatch is the final public name. The rename is cancelled.
- `v0.1.21` contains the Phase 22 cinematic Browse/player/Settings pass, new eclipse `NW` brand family, custom title bar, branded NSIS installer, and the Windows startup-window fix.
- The active follow-up branch is `frontend/phase-22-runtime-fixes`.
- Future lane PRs validate automatically but merge only when a reviewed commit carries the `Automerge: reviewed` trailer (generated by `git:finish -AutoMerge`), producing the `automerge` label. Releases remain owner-initiated through **Actions > Release**.
- Never replace, obscure, proxy, or place interactive controls over the official YouTube iframe. Custom controls belong below or beside it and use the official IFrame API.

## Frontend follow-up currently in progress

The runtime QA branch fixes:

- stale Browse responses, search-aware retry, and pagination/context races;
- touch-visible media actions and horizontally scrollable compact navigation;
- two-step destructive Settings resets and compact rail labels;
- persistent collapsible Reactions, Moment Notes, and room Discovery;
- persistent-message edit/delete, group rename, and the sender-side profanity preference.

Do not duplicate these changes in the backend lane.

## Backend capabilities already integrated

Migrations `0006` through `0019` cover explicit friends, blocks, consent-based presence, conversations/messages, moment notes, profile borders, Creator Clubs/bounties/moderation, notifications, club discovery/visibility/suspension, highlight timestamps, and the border identifier fix.

The UI already exposes:

- friend request/accept/decline/cancel/remove and presence;
- DMs, 2-30 member groups, unread state, sequence paging, realtime updates, group membership/roles/ownership, and system notices;
- private/friends/persistent-room moment notes below the player;
- unlocked profile-border selection;
- club discovery, visibility/suspension, bounties, submissions, voting, reports, moderation, and audit history;
- notification read/dismiss/clear controls;
- compliant highlight seeking and YouTube timestamp export (never video download).

All social services return `SocialResult<T>`. Treat `not-ready` as a hidden capability, not a visible error. Suggestions are co-watchers, not automatic friends. Message paging uses `seq`, not timestamps. Presence never exposes room codes. Soft-deleted messages remain cursor slots and render as tombstones.

## Backend work still needed for the requested social UI

See `PHASE_23_SOCIAL_UI_BACKEND_HANDOFF.md` for the typed contract. In short:

1. A privacy-safe public friend profile read with avatar, selected unlocked border, opt-in stats/achievements, mutual persistent rooms, friendship/block state, and Message/Invite permissions.
2. A blocked-user listing so the UI can present and safely execute unblock. `blockUser(id)`/`unblockUser(id)` alone cannot provide a complete management screen.
3. Safe conversation-member presentation fields (display name, avatar, selected border). The current group panel must fall back to a shortened UUID for non-friends.
4. An explicit accepted-friend-to-persistent-party invite/notification contract if a one-click Invite action is desired. Never disclose private room codes through presence.

Do not work around missing server data by deriving it from filtered directory/search results.

## Owner/deployment verification

- Verify migration `0019_list_my_clubs_visibility.sql` is applied.
- Redeploy `log-session` if highlights return no data: `supabase functions deploy log-session --no-verify-jwt`.
- Confirm the `v0.1.21` Release entry exposes installer, blockmap, and `latest.yml`; the tag and Release Action are green.
- Run packaged two-client create/join/sync/chat/reaction/queue/host-migration/reconnect tests.
- Run a real Discord Activity launch and high-latency drift test.
- Verify update installation from `v0.1.20` to `v0.1.21`.

## External limitation

Exact inspection of Figma node `YIrCEGqx0THUJOpmoYScYj/2235:2839` remains blocked by the Starter-plan MCP quota. The implementation uses the supplied screenshots and accessible reference patterns; this does not block functional testing.
